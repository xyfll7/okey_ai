import { HugeiconsIcon } from "@hugeicons/react";
import {
  DragDropVerticalIcon ,
  ArrowExpand01Icon ,
  ArrowUpIcon ,
  Pin02Icon ,
  Add01Icon ,
  Cancel01Icon,
  Tick02Icon,
  Copy01Icon,
  VolumeLowIcon,
  VolumeOffIcon,
  VolumeHighIcon,
} from "@hugeicons/core-free-icons";

const strokeWidth = 2

export const GripVertical = (props: any) => <HugeiconsIcon icon={DragDropVerticalIcon} {...props} strokeWidth={strokeWidth}/>;
export const ArrowExpand = (props: any) => <HugeiconsIcon icon={ArrowExpand01Icon} {...props} strokeWidth={strokeWidth}/>;
export const ArrowUp = (props: any) => <HugeiconsIcon icon={ArrowUpIcon} {...props} strokeWidth={strokeWidth}/>;
export const Pin = (props: any) => <HugeiconsIcon icon={Pin02Icon} {...props} strokeWidth={strokeWidth}/>;
export const Add = (props: any) => <HugeiconsIcon icon={Add01Icon} {...props} strokeWidth={strokeWidth}/>;
export const Cancel = (props: any) => <HugeiconsIcon icon={Cancel01Icon} {...props} strokeWidth={strokeWidth}/>;
export const Tick = (props: any) => <HugeiconsIcon icon={Tick02Icon} {...props} strokeWidth={strokeWidth}/>;
export const Copy = (props: any) => <HugeiconsIcon icon={Copy01Icon} {...props} strokeWidth={strokeWidth}/>;
export const VolumeLow = (props: any) => <HugeiconsIcon icon={VolumeLowIcon} {...props} strokeWidth={strokeWidth}/>;
export const VolumeOff = (props: any) => <HugeiconsIcon icon={VolumeOffIcon} {...props} strokeWidth={strokeWidth}/>;
export const VolumeHigh = (props: any) => <HugeiconsIcon icon={VolumeHighIcon} {...props} strokeWidth={strokeWidth}/>;
