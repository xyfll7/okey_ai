---
description: Backend project structure and where to edit (Tauri/Rust)
globs: src-tauri/**/*.{rs,toml,json,yml,yaml}
alwaysApply: false
---

# 后端结构（Tauri/Rust）

本规则仅在编辑后端文件时生效，主要覆盖 `src-tauri/` 目录。

## 主要目录

- `src-tauri/src/main.rs`：可执行入口，启动应用。
- `src-tauri/src/lib.rs`：Tauri 初始化与命令注册（`invoke_handler`）。
- `src-tauri/src/my_command.rs`：前端可调用命令（窗口、热键、翻译等）。
- `src-tauri/src/my_api/`：模型/API 管理与命令实现。
- `src-tauri/src/states/`：全局状态与会话状态。
- `src-tauri/src/utils/`：通用工具（日志、i18n、翻译管理等）。
- `src-tauri/tauri.conf.json`：窗口、构建路径、开发地址配置。

## 改动选址建议

- 改前端调用命令：优先改 `src-tauri/src/my_command.rs`，并同步检查 `src-tauri/src/lib.rs` 注册。
- 改模型接入或 API 调度：优先改 `src-tauri/src/my_api/`。
- 改全局状态：优先改 `src-tauri/src/states/`。
- 改后端文案：优先改 `src-tauri/locales/*.yml`。

## 与前端协作约定

- 新增命令必须加入 `invoke_handler`，否则前端 `invoke` 无法调用。
- 变更窗口路径时，保持与前端路由路径一致，避免页面无法加载。
