---
description: Frontend project structure and where to edit (React/Vite)
globs: src/**/*.{ts,tsx,js,jsx,css,scss,json}
alwaysApply: false
---

# 前端结构（React/Vite）

本规则仅在编辑前端文件时生效，主要覆盖 `src/` 目录。

## 主要目录

- `src/main.tsx`：前端入口，挂载路由与根组件。
- `src/routes/`：页面路由（file route）。
- `src/components/`：可复用组件与功能模块。
- `src/store/`：状态管理。
- `src/lib/`：工具函数、类型、事件定义。
- `src/i18n/`：前端多语言资源与初始化。
- `src/assets/`：静态资源。

## 改动选址建议

- 改页面布局或交互：优先改 `src/routes/`、`src/components/`。
- 改前端状态流转：优先改 `src/store/`。
- 改共享逻辑/类型：优先改 `src/lib/`。
- 改前端文案：优先改 `src/i18n/locales/*.json`。

## 与后端协作约定

- 新增页面路径时，确认 `src-tauri/tauri.conf.json` 中窗口 `url` 与路由一致。
- 调用新后端命令前，确认 Rust 侧已在 `.invoke_handler` 注册。
